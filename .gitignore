# ── Node / Frontend ───────────────────────────────────────────
node_modules/
dist/
dist-ssr/
*.local
*.tsbuildinfo

# Electron shell build artifacts (compiled per-OS backend binaries + packaged app output)
Vyline/apps/electron/release/
Vyline/apps/electron/resources/backend-bin/

# 参考入力（サードパーティのチェック済み LINE APK のローカルコピー。第三者の著作権物を含むため
# git 履歴には絶対に含めない）
line_droid/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── Bun ──────────────────────────────────────────────────────
.bun/

# ── Data (tokens / E2EE keys — never commit) ─────────────────
/backend/data/
data/
Vyline/backend/data/
.env
.env.*
!.env.example
**/desktop-e2ee-keys.json
**/desktop-e2ee-dump.txt
**/storage-*.json
**/tokens.json
**/chatdb-*.json

# ── Editor ────────────────────────────────────────────────────
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── OS ────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Project-specific ──────────────────────────────────────────
# リサーチ用途のため git 管理外 (リバースエンジニアリング成果物)
/research/
/source/

# ネットワークキャプチャ
*.har

# Archive
archive/

# Wails v3 build artifacts
**/build/bin/

# Session data (user-specific, never commit)
*.session.json

# Vyline-Search tools data (binaries, dumps, outputs)
tools/data/
*.exe
*.dll
*.pdb

# ── Editor / AI config (never commit) ───────────────────────────
.cursor/
.claude/
.agents/skills/handoff/
